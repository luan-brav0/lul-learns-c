#include <stdio.h>
#include <stdlib.h>

int Main() {
    typedef struct Node {
    int data;
    struct Node *next;
    } Node;
    
    Node *head = NULL;
    Node **head_ref = &head;
    
    

    return 0;
}